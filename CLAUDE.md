# 모꼬지 노션 RAG 챗봇

## 역할

10년차 AI 엔지니어로서 이 저장소를 다룬다. RAG 파이프라인의 각 단계(청킹 · 임베딩 · 검색 · 생성)가
서로 어디서 얽히는지 알고 있고, 어떤 변경이 재임베딩을 유발하는지 판단할 수 있다고 전제한다.

수치 없이 "좋아졌다"고 말하지 않는다. 개선을 주장하려면 골든셋으로 잰 값을 함께 제시한다.
측정하지 않은 인과는 가설이라고 명시한다.

## 왜 만들었나

주간 회의마다 같은 질문이 반복된다. "그거 어디 문서에 있더라", "그때 왜 그렇게 정했지" 같은
확인 작업에 회의 시간이 길어진다. 노션에 답이 있는데 찾는 비용이 크기 때문이다.

그래서 팀 노션을 자연어로 묻고 **출처 링크와 함께** 답을 받는 디스코드 봇을 만든다.
회의 중 근거를 즉시 확인하는 것이 목적이므로, 그럴듯한 답보다 **출처가 분명한 답**이 우선이다.

## 요구사항

| 항목 | 값 |
|---|---|
| 문서 형식 | 노션 |
| 문서 양 | 수백 건 (현재 952페이지 / 3388청크) |
| 업데이트 빈도 | 주 1회 |
| 언어 | 한국어 · 영어 (BGE-m3가 둘 다 처리) |
| 질문 유형 | 사실 확인 및 요약 |
| 질문 길이 | 중간 |
| 복잡도 | 단순 |
| 동시 사용자 | 1~2명 |
| 할루시네이션 | 거의 허용 안 됨 |
| 출처 표기 | 필수 |
| 최신성 vs 정확성 | **정확성 우선** |
| 예산 | 저예산 (전부 로컬) |
| 응답 지연 | 10초 이내 목표 |

동시 사용자가 1~2명이라 처리량 최적화는 의미가 없다. 지연과 정확도만 본다.

## 구조

```
Notion API → 마크다운 → 계층 청킹 → BGE-m3 → Qdrant
                                                 │
Discord Bot → 하이브리드 검색(dense+sparse, RRF) ─┘
                     │
                     ▼
              Ollama(로컬 CPU) → 답변 + 출처 링크
```

- `src/notion/` 수집, `src/indexing/` 청킹·적재, `src/retrieval/` 검색, `src/generation/` 프롬프트·LLM
- `src/retrieval/listing.py`는 "저번달 회의 목록" 류를 **LLM 없이** payload 필터로 처리한다.
  10초 예산에 여유가 없어서다. 여기서 처리되면 유사도 검색을 아예 타지 않는다.

## 절대 바꾸지 말 것

**외장 GPU가 없다. CPU 추론이고 RAM이 빠듯하다.**

`exaone3.5:2.4b`를 쓴다. 더 큰 모델로 올리지 말 것 — `qwen3:4b`는 느린 게 아니라
사고 과정만 3500자 쏟다가 토큰 상한에 걸려 **아예 답을 못 낸다.** 실측 결과다.

느리면 순서대로: `MAX_OUTPUT_TOKENS` 축소 → `SEARCH_TOP_K` 축소 → 더 작은 비추론 모델.

## 변경 전에 반드시 판단할 것: 재임베딩이 필요한가

이 저장소에서 가장 비싼 것은 CPU 임베딩이다. 무엇을 건드리는지에 따라 비용이 10배 차이 난다.

| 바꾸는 것 | 재임베딩 | 도구 |
|---|---|---|
| payload 값 (`doc_type`, `part`, `person`, `doc_date`) | **불필요** | `scripts.backfill_metadata` |
| 프롬프트 (`src/generation/prompt.py`) | **불필요** | 없음 |
| 청크 본문·`heading_path` | 필요 | `scripts.rechunk` |
| 청킹 파라미터, 임베딩 모델 | 필요 (전체) | `scripts.index` |

**payload는 벡터에 들어가지 않는다.** `store.py`가 `content_payload_key="text"`로 잡혀 있어
임베딩되는 것은 청크 본문뿐이다. payload는 필터와 프롬프트 렌더링에만 쓰인다.

따라서 "payload에 값을 넣었으니 검색이 좋아진다"는 틀렸다. 검색을 바꾸려면 본문이 바뀌어야 한다.

### 중단하면 인덱스가 깨진다

`pipeline.index_pages(replace_existing=True)`는 페이지를 **먼저 지우고** 청크는 64개 모아서 넣는다.
중간에 죽으면 지워졌는데 다시 안 들어간 페이지가 남는다. 실제로 회의록 5개를 이렇게 잃은 적 있다.

재인덱싱은 끝까지 돌린다. 중단했다면 `scripts.rechunk --dry-run`으로 확인하고,
페이지가 사라졌다면 `.cache/pages.json`과 남은 payload로 복구해야 한다.

## 평가

`src/eval/golden_set.jsonl`에 37문항. `python -m src.eval.run_eval`로 잰다.

**이 골든셋은 검색만 잰다.** `run_eval`은 `llm`을 import하지 않는다. 답변 문장이 맞는지,
환각인지, 거절을 제대로 하는지는 **측정 대상이 아니다.**

- 답이 여러 문서에 있으면 `answer_page_ids`에 전부 적는다. 노션에 같은 문서가 여러 경로에
  복사돼 있고, 백엔드 스터디는 같은 주제를 사람마다 따로 쓴다. 하나만 적으면 정답을 찾아와도 실패로 잡힌다
- 근거 없어서 거절해야 하는 질문은 **여기 넣지 않는다.** 미검출로 집계돼 Recall이 깎인다. 별도 셋이 필요하다
- `SEARCH_TOP_K`를 바꿔도 수치가 안 움직인다. `run_eval`이 `top_k=10`으로 고정해 부른다.
  대신 Recall@5와 @10을 비교해 이 값을 얼마로 둘지 정하는 데 쓴다
- **37문항이라 1문항이 2.7%p다.** 2문항(5.4%p) 이상 움직여야 개선으로 판단한다

`scripts.verify`는 별개다. 스모크 질의로 검색이 살아 있는지만 본다. 인덱싱 직후 점검용이다.

## 검색

- 하이브리드(dense+sparse)를 Qdrant 서버측 RRF로 융합한다
- 질문에 "프론트"/"백엔드"가 있으면 `part`로 좁힌 검색을 한 번 더 돌려 전체 결과와 **번갈아** 섞는다.
  이때 **전체 검색 쪽을 먼저 놓는다.** 좁힌 쪽을 앞에 뒀더니 이미 1위로 맞히던 질문 셋이 2위로 밀렸다
- 파트 필터는 전체 검색이 놓친 것을 건지는 보조 수단이지 주 경로가 아니다
- RRF 점수는 확신도가 아니다. 틀린 문서에도 1.0이 나온다. **점수 임계값으로 거절을 판단하지 말 것**

## 회의록 스크럼

회의록의 Scrum 섹션은 담당자 한 명당 청크 하나로 쪼갠다(`chunker.expand_scrum`).
표 하나에 파트원 전원 작업이 묶여 있으면 "누가 했어" 질문이 같은 주제의 전용 문서에 밀린다.

- 조각은 대개 한 줄이라 `merge_small_sections`에 걸린다. `atomic=True`로 병합에서 뺀다.
  합쳐지면 앞 조각의 `heading_path`를 물려받아 헤더와 내용이 어긋난다
- 호칭(`석준`)은 노션 `people` 속성 표기(`석준 허`)로 편다. `config.SCRUM_MEMBERS` 참조.
  혁수는 노션 표기가 `꽁이(혁수)`다. 어색해 보여도 `people`과 값을 맞춰야 한다
- **검색만으로는 부족하다.** 쪼개서 6위로 올려도 모델이 이름을 못 읽었다.
  `prompt._attribution`이 `담당: 석준 허 (완료)`를 별도 줄로 세운 뒤에야 답이 나왔다
- 부작용: 조각이 짧아 헤더 비중이 커서 무관한 질문에서 상위로 올라온다. MRR이 0.764 → 0.742로 내려갔다

적용 범위는 전체 3388청크 중 272개(8%)뿐이다. 다른 문서 유형의 작성자는 `people` payload에
있지만 프롬프트로 나가지 않는다. 회의록만 특별대우받는 상태는 의도한 설계가 아니라 미완이다.

## 민감 정보

2단 방어다.

1. 노션에서 integration을 개별 해제 — 공동계정, 계정 정보, 백엔드 yml, 카드, 회비, 멤버, one-on-one
2. `config.BLOCKED_PAGES` — 1번이 뚫렸을 때의 안전망

`config.EXCLUDED_PAGES`는 성격이 다르다. 민감하진 않지만 팀 문서가 아닌 것(동아리 모집글 DB,
데이터 백업)이다. 모집글 DB는 짧고 비슷한 페이지 수백 개로 검색을 희석시킨다.

둘 다 하위 트리 전체를 건너뛴다. 인덱싱 전에 `scripts.index --dry-run`으로 민감 페이지가
안 걸리는지 먼저 확인한다.

## 자주 쓰는 명령

```powershell
# 세 프로세스가 떠 있어야 봇이 동작한다
docker compose up -d
$env:OLLAMA_MODELS = "D:\Ollama\models"; D:\Ollama\ollama.exe serve
python -m src.bot.discord_bot

# 수집 대상 확인 (Qdrant 없이도 동작)
python -m scripts.index --dry-run

# 변경분만 재인덱싱
python -m scripts.sync

# payload만 다시 쓰기 (재임베딩 없음)
python -m scripts.backfill_metadata --doc-type-only --dry-run

# 청킹 규칙이 바뀐 페이지만 재임베딩
python -m scripts.rechunk --dry-run

# 검색 품질 평가
python -m src.eval.run_eval --show-misses

# CLI 질의
python -m scripts.ask --retrieval-only "질문"
```

## 코드 규약

- 주석은 **왜**가 자명하지 않을 때만 단다. 무엇을 하는지는 코드가 말한다
- 축약어를 쓰지 않는다 (`idx` → `index`)
- 일어날 수 없는 상황에 대한 예외 처리·폴백을 넣지 않는다
- 콘솔이 cp949라 노션 문서를 그대로 찍으면 깨진다. 스크립트 앞에
  `sys.stdout.reconfigure(encoding="utf-8", errors="replace")`를 넣는다
- 새 payload 필드를 `FILTER_INDEXES`에 등록하는 것은 **실제로 필터에 쓸 때만** 한다.
  쓰지 않을 인덱스를 미리 만들지 않는다
